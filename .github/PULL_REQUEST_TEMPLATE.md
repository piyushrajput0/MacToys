## What this changes

<!-- And why. If it fixes an issue, "Fixes #123". -->

## How you tested it

<!-- `make test` covers MacToysCore. Most of the rest can only be checked by
     hand, so say what you actually did: which macOS, which Mac, which feature. -->

- [ ] `make test` passes
- [ ] Tried it in a real build (`make run`)

## Anything to know

<!-- Needs a permission re-grant? Changes a default? Affects Intel differently
     from Apple silicon? Say so here. Delete if not. -->
